# Playtest Note: dtp-1782908353742-404f4b88-7315-43f9-bb52-93a6f4058475

**Date:** 2026-07-01  
**Session:** `dtp-1782908353742-404f4b88-7315-43f9-bb52-93a6f4058475`  
**Seed:** `496925901`  
**Log file:** `/Users/vladimirtitskiy/Dev/dtp-backend/logs/sessions/dtp-1782908353742-404f4b88-7315-43f9-bb52-93a6f4058475.json`  
**Frontend build in session:** `2c5cc19-dirty`  
**Purpose:** decide whether the session felt hard because of weak player strategy or because balance/mechanics need tuning.

---

## Summary

The player was not playing badly at the macro level. The business economy was healthy:

- Day 4, 13:21: `Trust 87`, `Clients 100`, `Debt 14`, `Value 223`, `Budget 2`.
- Quarter state: `quarter=1`, `dayInQuarter=4`, `quarterValue=223/75`.
- No `quarter_review` fired yet, confirming the intended `daysPerQuarter=5` behavior.

The real pressure came from operational flow, not from business collapse. Two tasks missed the release window from `In Progress` and generated fallout:

- `PAY-004 -> PAY-012`
- `REP-011 -> REP-018`

Both failures share the same shape: QA found bugs, rework was done, then the auto-created `Re-test changes after rework` step became the final gated step. The team ran out of stamina/time at that point.

Current working hypothesis:

```txt
The session was not lost because the player failed to create value.
It became tense because QA recheck sits at the end of the critical path.
```

This is an important distinction. "Play more risky" is a useful player-facing recommendation, but the repeated failure point may also be a structural trap in the QA recheck mechanic.

---

## Daily Results

| Day | Shipped | Clean | Risky | Dirty | Missed | Fallout Created | Unresolved Fallout | End Resources |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 1 | 2 | 2 | 0 | 0 | 0 | 0 | 0 | Trust 76, Debt 18, Value 69, Budget 4 |
| 2 | 4 | 3 | 1 | 0 | 1 | 1 | 1 | Trust 86, Debt 14, Value 177, Budget 6 |
| 3 | 2 | 0 | 2 | 0 | 1 | 1 | 2 | Trust 87, Debt 14, Value 223, Budget 2 |

Interpretation:

- Economy is ahead of the current goal.
- Trust is rising, debt is falling or stable.
- The board is accumulating fallout despite good macro output.
- The pressure is localized in task completion flow, especially QA/recheck/stamina.

---

## Confirmed Findings

### `DAYS_PER_QUARTER = 5` Is Working

On day 4 the run is still in quarter 1:

```txt
quarter=1
dayInQuarter=4
quarterValue=223
quarterGoal.value=75
```

There were no `quarter_review` events. This confirms the P0 regression is fixed for this session shape: quarter no longer equals day.

### PAY-004 Failure Chain

`PAY-004: Add permission review` timeline:

1. SRE work.
2. Analysis revealed backend.
3. QA found 2 bugs and converted them into rework.
4. Backend work completed and required QA recheck.
5. SRE rework exhausted Lena.
6. Backend bugfix completed.
7. Mira started `Re-test changes after rework`.
8. The task missed the release window and generated `PAY-012`.

Result:

```txt
PAY-004 missed deadline in In Progress
PAY-004 -> PAY-012
cause: missed_deadline
```

### REP-011 Failure Chain

`REP-011: Speed up report generation` timeline:

1. Outsource completed SRE critical work.
2. Backend work introduced a bug.
3. QA converted 1 bug into rework.
4. SRE bugfix completed and required QA recheck.
5. Mira started `Re-test changes after rework`.
6. Mira exhausted on that final step.
7. The task missed the release window and generated `REP-018`.

Result:

```txt
REP-011 missed deadline in In Progress
REP-011 -> REP-018
cause: missed_deadline
```

### REP-005 Correction

Initial interpretation said `REP-005` proved "early outsourced QA was wasted." The log does not support that.

Actual release:

```txt
REP-005 released
risky release
value +30
budget +2
trust +1
clients +1
debt -1
SRE blast radius reduced
```

The release was positive. It was risky, but not a failed example of wasted budget. The general caution still stands: QA before later dev/SRE can become stale. But `REP-005` itself is not evidence that early QA outsource is bad.

---

## Strategy Read

The player strategy was strong at resource management:

- high trust;
- perfect clients;
- low debt;
- high value;
- no dirty releases in the first three days.

The likely strategic overcommitment was trying to complete too much QA cleanliness on tasks that were already close to deadline. The better survival play may be:

- release some tasks as risky earlier;
- avoid letting near-complete work die in `In Progress`;
- save QA stamina for final checks, not early checks before dev/SRE is done;
- treat `Re-test changes after rework` as an expensive final gate;
- keep budget above outsource thresholds when possible.

But this should not be flattened into "the player is too cautious." The repeated failure happened at the same mechanic twice, so this is also a balance/design watchpoint.

---

## Current Engine Settings Snapshot

This snapshot reflects the current working tree after the `DAYS_PER_QUARTER` P0 fix.

### Time

```txt
TICK_MS = 500
GAME_MINUTES_PER_REAL_SECOND = 1
GAME_MINUTES_PER_TICK = 0.5
GAME_DAY_START_MINUTE = 08:00
RELEASE_TRAIN_GAME_MINUTE = 18:00
GAME_DAY_MINUTES = 600
DAYS_PER_QUARTER = 5
```

### Starting State

```txt
trust = 70
debt = 20
value = 0
clients = 100
budget = 4
processBoost = 0
quarterGoal.value = 75
quarterGoal.trust = 45
quarterGoal.rewardBudget = 2
starting team = analyst, backend, frontend, qa, sre
starting tasks = 2
```

### Quarter Resolution

Quarter goal passes when:

```txt
quarterValue >= quarterGoal.value
resources.trust >= quarterGoal.trust
```

On success:

```txt
budget += rewardBudget
processBoost += 5, capped at 25
```

On failure:

```txt
trust -= 8
```

Next quarter:

```txt
quarter += 1
dayInQuarter = 1
quarterValue = 0
quarterGoal.value = round(previousGoal * 1.18 + 20)
quarterGoal.trust = min(70, previousTrustGoal + 3)
```

Design decision as of this note: no hard cap on quarter goal yet. Endless survival pressure is intentional, but team growth/hiring becomes a required future counter-pressure.

### Board And Limits

```txt
columns = Backlog, In Progress, Done, Released
BACKLOG_LIMIT = 5
FALLOUT_BACKLOG_EXTRA_SLOTS = 2
MAX_FALLOUT_CHAIN_DEPTH = 2
DONE_REWORK_TRUST_COST = 4
```

`Backlog -> Done` is forbidden. Work must pass through `In Progress`. Tasks in `Done` freeze deadline and wait for the 18:00 release train.

### Spawn

```txt
FIRST_SPAWN_MIN_MS = 80000
FIRST_SPAWN_MAX_MS = 120000
SPAWN_INTERVAL_MIN_MS = 90000
SPAWN_INTERVAL_MAX_MS = 150000
LOW_WORK_SPAWN_MIN_MS = 25000
LOW_WORK_SPAWN_MAX_MS = 45000
BURST_INTERVAL_MIN_MS = 420000
BURST_INTERVAL_MAX_MS = 660000
```

Modifiers:

```txt
trust < 40 -> spawn interval * 0.85
trust < 60 -> spawn interval * 0.95
debt > 60 -> spawn interval * 0.9
backlog >= 3 -> spawn interval * 1.25
backlog >= 4 -> spawn interval * 1.6
active work <= 1 -> low-work spawn interval
```

Task kind weights:

```txt
feature 30
bug 14
techDebt 8
integration 14
incident 5
performance 10
compliance 9
```

Debt/trust pressure:

```txt
debt > 55 -> bug +14, techDebt +10, performance +10
trust < 40 -> incident +12
```

### Task Generation

```txt
pressure = incident ? random 4..6 : random 1..5
complexity = random 1..5
clarity = clamp(random 48..88 - trustNoise - debtNoise, 8, 92)
trustNoise = (100 - trust) * 0.45
debtNoise = debt * 0.1
deadlineMs = random 520000..780000 + complexity * 45000 - pressure * 15000
deadline minimum = 420000
value = round((8 + complexity * 4 + pressure * 3) * kindMultiplier)
```

Kind value multipliers:

```txt
feature 1.25
incident 0.75
techDebt 0.9
compliance 1.1
other 1.0
```

### Stamina And Work

```txt
WORK_SPEED_MULTIPLIER = 0.38
ANALYSIS_SPEED_MULTIPLIER = 0.24
WORK_STAMINA_DRAIN_BASE = 0.28
WORK_PRESSURE_STAMINA_DRAIN = 0.045
WORK_COMPLEXITY_STAMINA_DRAIN = 0.02
OFF_ROLE_STAMINA_DRAIN = 0.12
ANALYSIS_STAMINA_DRAIN_BASE = 0.23
ANALYSIS_PRESSURE_STAMINA_DRAIN = 0.025
ANALYSIS_COMPLEXITY_STAMINA_DRAIN = 0.012
```

Stamina recovery:

```txt
idle recovery = 0.12 stamina per game minute
night recovery = max(55, missingStamina * 0.8)
stamina <= 0 -> exhausted until next day
stamina < 20 -> burnout accumulates
```

Work speed depends on:

```txt
role fit
clarity factor
stamina factor
shock factor
off-role factor
processBoost
complexity
```

### QA, Bugs, And Recheck

Implementation bug probability:

```txt
probability =
  0.24
  + (100 - clarity) * 0.003
  + (100 - stamina) * 0.0025
  + lowQualityRisk
  + pressure * 0.035
  + complexity * 0.025
  + importanceRisk
  + offRole ? 0.28 : 0
  - roleFit * 0.075

clamped to 0.06..0.86
```

QA can discover bugs when:

```txt
open critical non-QA work exists -> 45% chance for 1 bug
quality < 45 -> random 0..1
quality < 70 -> 25% chance for 1 bug
```

When QA triages bugs:

```txt
bugs become important rework subtasks
bugfix role is chosen from visible/done non-QA roles
```

When non-QA work happens after QA:

```txt
changedAfterQa = true
testCoverage = min(testCoverage, 35)
ensureQaRecheckSubtask()
```

The recheck subtask:

```txt
title = Re-test changes after rework
role = qa
importance = important
revealed = true
```

Playtest watchpoint: this recheck currently sits at the end of the critical path and twice caused a missed in-progress task in this session.

### Late Release

```txt
LATE_RELEASE_GRACE_MS = 30000
```

If a task is overdue but reaches `Done`, it still releases. It applies useful effects but gets a `Value` multiplier penalty.

Late penalty:

```txt
first half-hour penalty = min(overdueHours, 0.5) * 0.1
late penalty = max(0, overdueHours - 0.5) * 0.16
pressure penalty = max(0, pressure - 3) * 0.025
total penalty *= kind/blast sensitivity
penalty capped by task kind
```

Max late penalty:

```txt
techDebt 25%
bug/performance 45%
feature 55%
integration/incident/compliance 65%
```

### Release Score

```txt
score =
  quality * 0.45
  + clarity * 0.25
  + testCoverage * 0.25
  + completedRatio * 25
  + (workDone ? 8 : -18)
  - openCritical * 22
  - openImportant * 9
  - hiddenOpen * 10
  - offRolePenalty
  - deadlinePenalty
  - bugs * 12
  - debt * 0.12
```

Value gain:

```txt
baseValueGain = round(task.value * score / 100)
valueGain = round(baseValueGain * lateMultiplier)
```

Budget gain:

```txt
score < 55 -> 0
otherwise floor(valueGain / 15)
```

Trust/client/debt are tiered by readiness and score. SRE safety changes blast multiplier:

```txt
SRE safety done -> blastMultiplier = 0.65
otherwise -> blastMultiplier = 1.15
```

### Missed Work

At morning transition, overdue non-released tasks in `Backlog` or `In Progress` are resolved:

```txt
Backlog miss -> source missed_backlog, cause ignored_work
In Progress miss -> source missed_in_progress, cause missed_deadline
minor task -> capped resource hit
major task -> source-linked fallout task
chain depth > 2 -> terminal consequence
```

This session confirmed the in-progress path twice.

---

## Recommendations For Next Playtest

1. Try a deliberately riskier style for one run:
   - move near-deadline tasks to `Done` as risky/late instead of chasing clean;
   - do not let almost-complete tasks die in `In Progress`;
   - treat `Re-test changes after rework` as expensive.

2. Track `Re-test changes after rework` failures:
   - if it kills a third important task the same way, treat this as a balance issue, not just player style.

3. Track QA stamina separately:
   - this session had QA at `0 stamina`, `burnout 10` by day 4 noon.

4. Do not over-index on `REP-005` as evidence against early outsourced QA:
   - that release was positive.

5. Re-check quarter goals after a full 5-day quarter:
   - this session already had `223/75` by day 4, so the first goal is currently too low after the `daysPerQuarter=5` fix.

---

## Open Balance Hypotheses

### H1: QA Recheck Is Structurally Too Expensive

Evidence:

- `PAY-004` and `REP-011` failed on the same final recheck shape.
- The recheck is automatically created after post-QA dev/SRE work.
- It appears late, when deadlines and QA stamina are already under pressure.

Possible future knobs:

- make recheck `optional` instead of `important` in some cases;
- let recheck be shorter than full QA;
- let stale QA remain a risky release state without forcing a visible final task;
- reduce stamina drain for recheck;
- make recheck progress partially carry across exhaustion/day boundary.

### H2: First Quarter Goal Is Too Low After `DAYS_PER_QUARTER=5`

Evidence:

- Day 4 before release: `quarterValue=223/75`.
- No quarter review had fired yet.

Do not tune immediately from one session, but first-quarter target likely needs recalibration once weekly/monthly/quarterly goals replace the placeholder.

### H3: Budget Feels Binary Around Thresholds

Evidence:

- Budget fell to `2`.
- Multiple outsource attempts failed at `need 3/4/6`.

This may be good friction, but if it feels like dead UI rather than a strategic shortage, add clearer affordance or cheaper limited fallback.
